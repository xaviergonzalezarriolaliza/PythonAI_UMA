// Define an array with type annotation
const fruits = ['apple', 'banana', 'cherry'];

// Use forEach to iterate over each element
fruits.forEach((fruit, index) => {
    console.log(`Index ${index}: ${fruit}`);
});
console.log("Finished listing fruits.");
